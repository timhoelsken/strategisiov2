SVN commit
	+	=>	add new stuff
	-	=>	delete stuff
	%	=>	change stuff

variables
	static variables	CAPITAL_LETTERS
	local variables		with prefix "tmp" => tmpLocalVariable
	parameters			with prefix "a" => aParameter
                        boolean with prefix "is" => isBoolean
	attributes			without prefix => thisIsAnAttribute

class names
	LongClassNameBecauseLongClassNamesAreVeryNice

method names
	longMethodNameBecauseNoThatIsNotFunny()